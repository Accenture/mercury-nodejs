# Session (2026-08-23T02:46:01.000Z)

**Agent:** Claude Code

## Summary

**The primitive in-process event bus implemented — lock-step twin of mercury-python's
`957d6b7` — branch `feature/primitive-event-bus`, commit `da8ce4c`.**

- `bus.ts` (internal): per-route FIFO Mailbox + `instances` worker loops (faithful
  parameter; the Semaphore class is gone). `deliver` (RPC, ttl-bounded → 408, dead-work
  skip via the settled flag) + `publish` (drop-n-forget, 202-shape ack). Worker owns the
  invocation pipeline (runWithTrace, AppException → portable error, execTime, error
  logging for un-replied publishes). **Event-loop lifecycle ruling discovered in test:
  an in-flight RPC's deadline timer is deliberately REFERENCED (pending work holds the
  process open, at most until its deadline); an idle bus holds no handles** — the
  unref'd-everything first cut drained the loop mid-test (cancelledByParent).
- HTTP host = thin ingress; PostOffice without an endpoint = local ingress (private
  faithful: in-app callable, wire keeps 403); ctor gained the registry parameter.
- Demo chain pair added; README + CHANGELOG document local calls, no-spill/no-cap
  posture, the workflow boundary, and the lifecycle ruling.
- Pins: test/bus.test.ts — the same eight as the python twin. Live wire proof green:
  `hello.chain` over `/api/event` → `{'text': 'polyglot!', 'language': 'node.js'}`;
  direct private → 403.

Follow-up commits: `31c8b0b` (aligned with the python twin's Sonar round — comment
wording + static execute) and `3d866f1` (unshadowed inner PostOffice in the trace-chain
test).

Gates: **tests 39/39** (tsc + node:test). Branch = 3 commits, pending Eric's PR gate.

## Memory References

- referenced: (python-repo id) thread-primitive-event-bus (the ratified design; this repo
  implements the twin)
- created: thread-primitive-event-bus-node (tier: working)
