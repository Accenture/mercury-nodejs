# Session (2026-08-23T03:19:20.000Z)

**Agent:** Claude Code

Actuator endpoints on `feature/primitive-event-bus` — the node half of Eric's directive
(wrapper apps deploy as Kubernetes PODs, so they carry the engines' operational surface).
Commits `342a854` (feature) + `7a8b12c` (spelling + origin-test assert, aligning with the
python twin's IDE/Sonar round). Python twin: `56c002c` + `a674198` — the full design
narrative lives in mercury-python's log `2026-08-23-031558` (single-authored, shared
design); this log records the node-side specifics.

## What shipped (node specifics)

- `src/actuator.ts`: GET `/info`, `/info/routes`, `/env`, `/health`, `/livenessprobe`;
  shapes mirror the Rust engine's actuator (the approved minimalist port of Java
  `ActuatorServices`), byte-symmetric with the python twin except the runtime block
  (`{language: "node.js", node: process.version, mercury_composable: VERSION}`).
- Health check functions speak the engines' `type=info` / `type=health` interface
  contract through the event bus (Eric's ruling); `mandatory.health.dependencies` /
  `optional.health.dependencies` config; UP 200 / DOWN 400; `/livenessprobe` follows the
  most recent health outcome. Engine formats verbatim (origin = UTC yyyyMMdd + 32-hex
  uuid per the Java reference; elapsedTime boundary quirks pinned).
- `Actuator.handle(pathname, res)` single dispatcher; `server.ts` routes every GET
  through it (unknown GET → 404 text). The python twin adopted this dispatcher shape in
  its Sonar round — the twins are now structurally symmetric.
- `src/version.ts` becomes the single TS-side version source (`index.ts` re-exports) —
  breaks the actuator → index import cycle.
- The pre-release `/health` "OK" scaffolding endpoint is replaced by the real one.
- `examples/demo-app.mjs` gained `demo.health` (private, contract-speaking); README
  "Actuator endpoints" section (+ K8s probe wiring); CHANGELOG entry.

## Verification

- 48/48 (`npm test`: tsc + node --test) — 10 actuator pins mirroring the python set
  (five shapes, liveness-follows-health, optional-failure-never-downs, teach-message,
  elapsed/origin vectors); re-run after the alignment round.
- Live drive: `node dist/src/cli.js examples/demo-app.mjs --port 8398
  -Dmandatory.health.dependencies=demo.health` → all five endpoints engine-shaped,
  `/health` UP with the full dependency entry, symmetric with python on 8399.

## Follow-up round (Eric's three asks, commit `8ed4266`; python twin `7d5ca6c`)

- `log.format` gains `compact` (single-line JSONL); `json` becomes pretty-printed
  (indent 2) — matching the Java engine's JsonAppender/CompactAppender pair (same data
  map, two renderings). Full rationale in the python twin's log `2026-08-23-031558`.
- Sample `examples/resources/application.yml` (port 8087, demo.health dependency,
  opt-in /env lists) — auto-loaded by the CLI's next-to-app fallback; the demo runs
  with plain `node dist/src/cli.js examples/demo-app.mjs`. README quick start teaches
  the `-D` override syntax instead of `--port`.
- Live drive through the yml: /health UP under the demo-app identity, /env opted-in
  keys, `-Dlog.format=compact` emits JSONL. Gates: 48/48.

## Memory References

- created: thread-actuator-endpoints-node
- referenced: thread-primitive-event-bus-node, bp-publish-interop-gate,
  vision-mercury-nodejs
