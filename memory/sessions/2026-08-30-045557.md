# Session (2026-08-30T04:55:57.000Z)

**Agent:** Claude Code

## Summary

**The progressive-rendering round, shipped as the v4.12.0 milestone** - the Node.js
twin of the python round (structurally identical, same day). Merged as
[PR #90](https://github.com/Accenture/mercury-nodejs/pull/90) true merge `40a9f8f`
carrying gated `c0c28a6`; tree verified == gated; branches deleted both ends.
Version 0.1.0 -> **4.12.0**, aligning with the engine lock-step line and jumping
the npm lineage past the legacy pre-composable 4.3.x.

What shipped (engines' contract, python-twin structure):

1. **Event streaming**: `EventStreamWriter` producer (interceptor functions), the
   streaming `/api/event` host speaking the hybrid envelope-mode SSE dialect
   (pinned 406/503/408; single-shot byte-identical), `PostOffice.stream()` async
   iterator with conformance guards, `streamTo()` relay - on the primitive bus's
   new reply_to mechanism (generic `Mailbox<T>`, per-request reply sinks,
   documented `raceMs` reuse contract for the un-cancellable-promise pitfall).
2. **Business correlation-id continuity**: `my_cid` tag fill-stamp outbound,
   `my_correlation_id` view injected at delivery, `runWithTrace()` (now
   exported) carries it.
3. **Span lineage**: 16-hex spans per traced execution parented on the inbound
   envelope span; outbound/segment span stamps (touch parity + traceparent);
   the `rpc` tag suppresses RPC-leg datasets; the engines' distributed-trace
   dataset emitted on the `distributed.tracing` stdout stream; sender
   attribution (host fills `event.api.service`; context carries the route).
4. **Application log context**: packaged `default-log-context.yaml` (copied into
   dist/src at build - the `files` field ships dist only), resources override,
   `app.log.context` flag, `updateContext()` API; `context` block on
   json/compact lines; structured (object) log messages render as real JSON.
5. Interop report copy + streaming guide + demo echoes (trace_id +
   my_correlation_id in eof metadata; a context-bearing log line).

**Durable lessons (node-specific):**

- The node test runner's completion records ride stdout: a test that patches
  `process.stdout.write` must TEE, never swallow - a swallowed buffer silently
  dropped a sibling test from the run report (72 ran, 71 reported).
- Long-lived bus worker loops start under the creating caller's
  AsyncLocalStorage store - detach with `runWithTrace(undefined, ...)`
  (signature widened to `TraceInfo | undefined`).
- tsc does not keep a `let` narrowing across a deferred closure - capture a
  `const` for worker-loop closures.
- Sonar S2871: `.sort()` on string arrays needs `(a, b) => a.localeCompare(b)`.

Gates at merge: tsc clean, node --test 76/76, mkdocs strict; live drives against
both engines and the python twin recorded in the interop report.

## Memory References

- referenced: bp-publish-interop-gate (substance updated: v4.12.0 merged; npm
  publish still Eric-gated), stack-typescript-esm (version substance 0.1.0 ->
  4.12.0)
- created: ot-streaming-telemetry-round-20260830 (tier: working)
