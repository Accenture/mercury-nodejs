# Session (2026-08-22T17:53:27.000Z)

**Agent:** Claude Code

## Summary

Ran the **memory smoke test** at the maintainer's direction, as designed: a genuinely
fresh-context agent read ONLY the allowed memory files (protocol, instructions,
continuity, latest sessions, archive index — no source, no README, no vision) and answered
all 12 questions. **Score: 11/12.** The one gap (Q9) was precise and real: memory
described the receiving endpoint (`POST /api/event`) but never the engine-side wiring —
the `yaml.event.over.http` route→URL map lived only in README, which the test rightly
forbids. Fixed per the test's rule (fix the memory, never soften the question): a new
"How an engine calls a function here (the wiring)" section in `memory/instructions.md`
records the zero-caller-code binding, `preload()` registration, and end-to-end trace
carriage. The agent also noted a passed-but-partial Q2 (Stack & Tools carried no pinned
versions) — the stack fact now records TypeScript ^5.6, `@types/node` ^22, and Node ≥ 20
from `package.json` `engines`. Result log appended.

## Memory References

- referenced/updated: stack-typescript-esm (substance enriched with pinned versions)
