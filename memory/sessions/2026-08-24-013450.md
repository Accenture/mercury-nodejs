# Session (2026-08-24T01:34:50.000Z)

**Agent:** Claude Code

**The node quality-review round (Eric's screenshot-driven pass — the twin of
mercury-python's PR #16 round), executed on `chore/quality-polish`.** 14 commits
(incl. Eric's LICENSE year refresh at the base and the EventEmitter rationale
doc-comment from the standing agenda); `npm test` 48/48 after every commit; live
drives where the suite has no coverage (demo, CLI). Pending Eric's PR gate.

## Fixed, by rule

- **Editor determinism:** `"types": ["node"]` pinned in tsconfig (the node:crypto/
  process "cannot find name" errors were Eric's stale TS server — restart fixed;
  the pin makes editor resolution explicit). Lesson: SonarQube-for-IDE analyzes
  OPEN FILES only — every flagged rule got a repo-wide sweep.
- **S2933** readonly on construction-only fields: Mailbox.items/waiters,
  EventBus.mailboxes, FunctionRegistry.services, AppConfig.overrides.
- **S7773** Number.parseInt over the global (cli, server).
- **config.ts batch:** S6606 (`instance ??=`), S6582 (`!key?.trim()`),
  S6594 (hoisted WHOLE_REF.exec; twin fixed in log.ts).
- **S6551 arc (the big one):** no default Object stringification — evolved into ONE
  shared `asText` exported from envelope.ts (config's copy deleted; actuator's
  asList converted). **Durable TS lesson: negating `typeof x === 'object'` on an
  `unknown` widens the remainder to `{} | undefined`, which re-admits objects —
  the rule only clears with POSITIVE primitive narrowing** (string/number/bigint/
  boolean/symbol enumerated; function → toString; object/null → JSON; undefined
  literal). Rendering now aligns with the python twin's `str()` behavior better
  than `[object Object]` ever did. Demo: text validated as string (non-string →
  teaching 400, live-proven).
- **S3776 ×2** house helper-extraction: envelope.fromMap 23 → copyStringFields/
  copyNumericFields; actuator.checkServices 20 → per-route checkService.
- **S7722** the call-site stack probe's Error carries a message.
- **S8786 ×2 (two rounds!):** colon-exclusion in the class was NOT enough — an
  unanchored scanning regex with overlapping classes stays polynomial on
  non-matching input. callSite now parses right-to-left with lastIndexOf —
  strictly linear. Lesson: for tail-parsing, string ops beat regex scanning.
- **S6571** Handler returns honest `unknown` (the union was absorbed); teaching
  moved to the doc comment.
- **S7785** cli.ts uses top-level await (ESM, node>=20); error exits re-verified 1.
- **S9073** composite test assertions split ×3 (envelope + 2 siblings in server).
- **bus.ts header** now records the anycast-vs-broadcast EventEmitter rationale
  (the standing agenda item from Eric's pre-workshop question).

## Loose-ends round (same branch, after the smells)

Eric's three engine-parity items, signatures extracted from the Java repo (commit
`956c706`; python twin `da60593` on feature/actuator-polish): `GET /` index page
(platform-core public/index.html style, embedded — no static file service); actuator
JSON pretty-printed (SimpleMapper's default pretty Gson); unknown paths and non-GET
methods answer SimpleHttpUtility's `{status, message, type: "error"}` with
`Resource not found`. 51/51 incl. three new pins; live drive verified all three shapes.

## Charset ruling (Eric)

Content types verified live on both wrappers: application/json on every actuator
endpoint and the error shape, text/plain only on /livenessprobe (engine parity), text/
html on /. Eric ruled the twins match EACH OTHER with explicit `; charset=utf-8`
(node updated, `a73332f`) rather than python going bare like the Java engine.

## Memory References

- referenced: thread-node-quality-review, thread-primitive-event-bus-node,
  thread-actuator-endpoints-node, bp-publish-interop-gate
- updated substance: thread-node-quality-review (executed, pending PR)
